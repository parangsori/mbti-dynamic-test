export default function ServiceCopyright({ className = '' }) {
  return (
    <p className={`text-center text-[11px] font-semibold tracking-[0.04em] text-slate-600 ${className}`}>
      © 2026 todaymbti.com
    </p>
  );
}
